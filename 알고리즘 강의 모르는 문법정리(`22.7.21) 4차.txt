   1. caharAt()
    - String으로 저장된 문자열 중 한 글자만 선택해서 char타입으로 변환해준다.

   2. replaceAll 예시
   arrayList.replaceAll(s-> s.charAt(n)+s);
   System.out.println(arrayList);
   Collections.sort(arrayList);
   arrayList.replaceAll(s-> s.substring(1));
   list = arrayList.toArray(list);

   3. 클래스 이름 바꾸기
   SHIFT + F6 으로 바꾸면 전체가 바뀐다.